{
  "name": "equilibrium-monorepo",
  "version": "1.0.0",
  "description": "Monorepo workspace for Equilibrium AI Investment Research Agent",
  "private": true,
  "scripts": {
    "install:backend": "npm install --prefix backend --legacy-peer-deps",
    "install:frontend": "npm install --prefix frontend --legacy-peer-deps",
    "install:all": "npm run install:backend && npm run install:frontend",
    "build:frontend": "npm run build --prefix frontend",
    "start:backend": "npm start --prefix backend",
    "start:dev:backend": "npm run dev --prefix backend",
    "start:dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run install:all && npm run build:frontend",
    "start": "npm run start:backend"
  }
}
